mutation deleteAttachment($id: ID!, $documentId: String!, $changeId: String!, $contractId: ID!, $change: _CreateAttachmentChangeInput!) { packet { createAttachmentChange(input: $change) { id } updatePdsContract(input: { id: $contractId, attachmentsHead: $changeId }) { id } updateAttachment(input: { id: $id, current: false }) { id } } }
